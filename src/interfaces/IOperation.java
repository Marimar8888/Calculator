package interfaces;

public interface IOperation {
    //Interface para la implementacion del metodo donde recibe los números para realizar la operación concreta
    //Devuelve el resultado de la operación
    int performOperation(int firstNumber, int secondNumber);
}
